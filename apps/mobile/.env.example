# Publiczny origin REST API (NestJS). Bez sekretów w EXPO_PUBLIC_*.
# Development / LAN: http://localhost:3001 | emulator http://10.0.2.2:3001 | telefon http://<LAN-IP>:3001
# Preview / production (EAS): https://przepisy-jacka-production-ae86.up.railway.app
#   (ustawiane w eas.json → env, nie commituj lokalnego .env z produkcją bez potrzeby)
#
# Po stronie lokalnego API (apps/api/.env), nie Railway:
#   AUTH_TRUSTED_ORIGINS=http://localhost:3000,mojakuchnia://
#   MEDIA_STORAGE_DRIVER=memory
EXPO_PUBLIC_API_URL=http://localhost:3001
